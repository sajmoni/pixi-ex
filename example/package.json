{
  "name": "example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "refresh": "yarn cache clean && rm -rf node_modules && yarn --no-lockfile",
    "start": "parcel ./index.html --no-autoinstall --no-cache"
  },
  "dependencies": {
    "pixi-ex": "file:../pixi-ex.tgz",
    "pixi.js": "5.2.1"
  },
  "devDependencies": {
    "parcel-bundler": "1.12.4",
    "parcel-plugin-static-files-copy": "2.3.1"
  }
}
